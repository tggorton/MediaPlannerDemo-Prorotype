export function MetadataAnalysis() {
  return (
    <>
      <div className="ptitle">Metadata Analysis</div>
      <div className="psub">
        TODO: port from <code>js/metadata-analysis.js</code> (2,036 lines).
      </div>
      <div
        style={{
          padding: '24px',
          background: 'var(--surface)',
          border: '1px dashed var(--border-md)',
          borderRadius: 12,
          color: 'var(--muted)',
        }}
      >
        This page is a stub. The vanilla version renders here via{' '}
        <code>renderMetadataAnalysis()</code>. Conversion of this file is queued as
        a follow-up task.
      </div>
    </>
  );
}
